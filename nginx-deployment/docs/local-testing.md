# Local Testing Notes

The Nginx deployment scripts are designed for an Ubuntu VM or server where Nginx is installed.

If the scripts are run on a local Mac without Nginx, validation may fail at this step:

```text
ERROR: Nginx is not installed or not available in PATH.
