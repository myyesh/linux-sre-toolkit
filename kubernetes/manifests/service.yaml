apiVersion: v1

kind: Service

metadata:
  name: sre-ops-site-service

spec:
  type: NodePort

  selector:
    app: sre-ops-site

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
